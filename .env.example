# Copy this file to .env.local and fill in values. NEVER commit your real keys to git.
# If you accidentally committed a key, rotate/revoke it immediately.

# API key for the Gemini/Generative AI service
GEMINI_API_KEY=

# Optional: project number or id if required by your provider
GEMINI_PROJECT_NUMBER=

# The backend proxy will forward requests to this URL. Set to your provider's endpoint.
# Example (do NOT use unless correct for your provider): https://generativelanguage.googleapis.com/v1beta2/models/your-model:generateText
GEMINI_API_URL=

# Local server port used by Next.js (optional)
PORT=3000
